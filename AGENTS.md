# AGENTS.md

- Commit small, commit often, and sync with remote

## Workspace
- Uses `gh`. See `docs/agents/issue-tracker.md`.
- Uses default triage labels. See `docs/agents/triage-labels.md`.
- Is a single-context repo. See `docs/agents/domain.md`.

## Tools
- `herdr` -> agent multiplexer
- `searxngr`, `w3m` -> web search, web fetch

## Models
Follow when interacting with other agents or yourself
- `gpt-5.6-sol` -> expensive, slow, intelligent
- `gpt-5.6-luna` -> cheap, fast, competent
